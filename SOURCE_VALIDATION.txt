Attainment Question-wise CIA v8 source validation
=================================================

Validated on 2026-08-15:
- All server JavaScript files pass `node --check`.
- All 30 frontend JS/JSX source files parse successfully using TypeScript JSX transpilation.
- All relative frontend imports resolve to existing files.
- Department-wise CIA import UI supports multiple workbook selection and sequential import.
- Existing v7 CIA datasets are backfilled into department verification records.
- CIA question/activity MongoDB unique indexes are department-aware.
- Supplied English CIA workbook was inspected for department/source structure.
- Zero-observed question maximum fallback leaves only completely missing source datasets unresolved rather than guessing them.

A full Vite bundle was not produced in this Linux validation environment because the available project node_modules were installed on Windows and do not include Rollup's Linux native optional package. Source syntax/import validation passed.
